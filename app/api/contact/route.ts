import {NextResponse} from 'next/server'; import {sendEmail} from '@/lib/email'; export async function POST(req:Request){try{const b=await req.json();if(!b.email||!b.message)return NextResponse.json({error:'Email and message required'},{status:400});await sendEmail(process.env.SUPPORT_EMAIL||b.email,`Smart Portfolio Builder contact from ${b.email}`,`<p>${String(b.message).replace(/</g,'&lt;')}</p>`);return NextResponse.json({ok:true});}catch(e:any){return NextResponse.json({error:e.message},{status:500});}}
