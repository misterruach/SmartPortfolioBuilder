import {ALL_TEMPLATES} from '@/lib/templates'; export default function Templates(){return <main className="page"><div className="builder-head"><span className="eyebrow">DESIGN GALLERY</span><h1>Choose your direction.</h1><p>100 portfolio templates and 100 resume templates share the same structured content system.</p></div><div className="template-grid">{ALL_TEMPLATES.slice(0,40).map(t=><div className="glass template" key={t.id}><div className="template-art"><span>{t.type==='portfolio'?'PORTFOLIO':'CV'}</span><b>{t.name}</b></div><div className="template-meta"><div><h3>{t.name}</h3><p>{t.category} · {t.tier}</p></div><button className="btn btn-ghost">Preview</button></div></div>)}</div></main>}
