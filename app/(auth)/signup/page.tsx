'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Signup(){const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [done,setDone]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();const {data,error}=await createClient().auth.signUp({email,password,options:{data:{name}}});if(error)setError(error.message);else if(data.session)r.push('/dashboard');else setDone(true);}return <main className="auth"><form className="glass card" onSubmit={submit}><span className="eyebrow">START FREE</span><h1>Create your workspace.</h1><p>Build one portfolio or CV free.</p><input className="input" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} required/><input className="input" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" placeholder="Password (8+ characters)" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<div className="error">{error}</div>}{done?<div className="success">Check your email to confirm your account.</div>:<button className="btn btn-primary">Create account</button>}<small>Already have an account? <Link href="/login">Sign in</Link></small></form></main>}
