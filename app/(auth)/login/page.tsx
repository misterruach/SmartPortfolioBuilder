'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else r.push('/dashboard');}return <main className="auth"><form className="glass card" onSubmit={submit}><span className="eyebrow">WELCOME BACK</span><h1>Enter your workspace.</h1><p>Continue building your professional identity.</p><input className="input" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<div className="error">{error}</div>}<button className="btn btn-primary">Sign in</button><small>New here? <Link href="/signup">Create an account</Link></small></form></main>}
